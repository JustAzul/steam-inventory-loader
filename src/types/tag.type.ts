export type Tag= {
    // eslint-disable-next-line camelcase
    internal_name: string,
    name: string,
    category: string,
    color: string,
    // eslint-disable-next-line camelcase
    category_name: string
    }